{
  "name": "markk",
  "version": "1.0.0",
  "description": "Llenado automático de formularios desde fotos con IA",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "postinstall": "playwright install chromium || true"
  },
  "dependencies": {
    "playwright": "^1.61.1"
  },
  "engines": {
    "node": ">=18"
  }
}
